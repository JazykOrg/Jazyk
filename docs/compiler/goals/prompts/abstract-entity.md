This goal abstracts one entity over a limit: requirements per entity (50 soft, 80 hard), children per entity (10, 20), or states per state machine on this subject (12, 20); the change line names the limit and the count, and past the hard threshold the goal is mandatory. Load the entity in full and every requirement on it, gathered across all documents, before deciding anything: the cone is quiet, so the counts are final for this build, and you abstract once, holistically. The abstraction skill says how. Split by cohesion of the requirements: group them by what they are about (a sub-concept, a phase, a role, an interface, a lifecycle stage, a recurring noun), and a group is a sub-entity only when the documents already have a noun for it and its statements are about that noun directly. Respect scopes (a split never crosses one) and stated containment (a composition edge sets parent, and no sub-entity contradicts it). Search before creating: a noun the documents have may already be an entity, which is re-parented with update_entity parent, never minted twice; it has no new fact of its own to ratify, so stage the whole-part statement for it with upsert_requirement passing provenance derived (from this entity and the part, reasoning the cohesion) in place of section and quote, entities this entity and the part, and one composition edge from this entity to the part. Introduce each sub-entity with upsert_entity passing provenance derived in place of mention: parent this entity, the definition written as the sentence the documents should gain in their own vocabulary, a stereotype when the medium calls for one, from this entity and the requirements it groups, reasoning the cohesion it follows and the section the proposal targets; a derived entity is invented until ratified. Move the detail: re-point each grouped requirement with update_requirement, passing only id and entities (the sub-entity in place of this entity when the statement is about the part, this entity kept when it is about the whole), keeping transition.subject and every edge end in entities; attributes move with their concept through update_entity on the sub-entity. Over the children limit, introduce an intermediate parent the same way; over the state limit, split the subject into a sub-entity per phase and re-point each transition statement's entities and transition.subject to the phase whose states it uses, never merging states to lower the count. The parent keeps the statements about the whole; refresh its definition to name the parts when the documents' wording does, and collapse it in the coarse views that do not need the internals. Stage the docs proposals through the definitions: the harness files one ratification proposal per derived fact for the ratify goal, so write each definition as prose the author would keep, name the target section in the reasoning (the one introducing the parent, or a new sub-document when that section is over its size limit), one sentence per fact, never a bundle. When the structure you propose sits at the top of the tree (components where the documents state none), file report_diagnostic rule decision on the entity with a prompt: one question, one answer option per candidate structure, freeform true; the human's ruling decides before generation groups by it. Never invent a concept the documents cannot support, never delete the parent, never move a statement about the whole, and never re-split what a compile review merged back (a flip parks the pair as unstable-derivation, blocked on a human). When the requirements cohere into no named groups, or the pressure is one oversized section's (section-too-large is the author's to split), fail the goal with that reason, recommending a decree on the entity's own limit when the count is honest. The gate: sub-entities exist with parent this entity and derived provenance, the grouped detail is moved under them, the docs proposals are staged for ratify (the whole-part statement for each re-parented existing entity), and the count is within the limit.
