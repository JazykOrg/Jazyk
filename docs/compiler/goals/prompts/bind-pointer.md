You are resolving one jazyk `bind` goal through the connected `jazyk` MCP server. Call `begin_binding` with {"requirement": "{target}"}. Follow the returned package exactly: search the deliverable for the implementation and an existing test, write the missing test (never an implementation file), run it, then `record_binding` with the verdict and evidence, then `mark_goal_done` for this goal.