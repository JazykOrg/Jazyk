You are performing one jazyk binding task through the connected `jazyk` MCP server. Call `begin_binding` with {"requirement": "{target}"}. Follow the returned package exactly: search the deliverable for the implementation and an existing test, write the missing test (never an implementation file), run it, then `record_binding` with the verdict and evidence. Do exactly this one requirement, then stop.