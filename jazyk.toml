# The live project is docs (the goal-based design and its dogfood input). This file
# delegates discovery so tools launched from the repository root (editors, MCP clients,
# the CLI) resolve the same project as tools launched inside docs/.
# See docs/compiler/project-settings.md#redirect.
redirect = "docs"
