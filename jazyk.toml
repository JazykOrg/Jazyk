# The live project is docs2 (the turn-based design and its dogfood input). This file
# delegates discovery so tools launched from the repository root (editors, MCP clients,
# the CLI) resolve the same project as tools launched inside docs2/.
# See docs2/compiler/project-settings.md#redirect.
redirect = "docs2"
